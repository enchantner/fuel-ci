scenario: build_mirror

objects:
  repositories:
    fuel_foo:
      meta:
        packages_file: "sample_packages.txt"
      url: "https://github.com/enchantner/fuel-foo.git"
      path: "fuel-foo"

  artifact_storages:
    local_storage:
      drivers:
        storage: "localfs"
      url: "poc_storage"

build_objects:
  mirror:
    foo:
      drivers:
        repo: null
      path: "fuel-foo"
  artifacts:
    foo:
      drivers:
        compress: "tar"
      version: "0.1"
